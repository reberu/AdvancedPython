# Определить, какие из слов «attribute», «класс», «функция», «type» невозможно записать в байтовом типе.
# Байтовые типы могут содержать только символы ASCII,
# соответственно переменные bytes1 и bytes2 невозможно записать в байтовом типе.
bytes1 = b'класс'
bytes2 = b'функция'
